# This file is generated by torch2vk.export. Do not edit by hand.
"""Qwen3-ASR fused single-token gate/up projection plus SwiGLU."""

from __future__ import annotations

from torch2vk.runtime.shader import (
    IOKind,
    PushConstantFieldSpec,
    PushConstantSpec,
    PushConstantType,
    ShaderContract,
    ShaderExecutionRequirements,
    ShaderVariant,
    TensorContract,
    TensorFieldSpec,
)
from torch2vk.vulkan.shader_execution_requirements import SubgroupRequirements


QWEN3_ASR_TEXT_GATE_UP_SWIGLU_T1_F32 = ShaderVariant(
    name="qwen3_asr_text_gate_up_swiglu_t1_f32",
    family="qwen3_asr.text",
    contract=ShaderContract(
        class_name="Qwen3AsrTextGateUpSwigluT1F32Program",
        shader_name="qwen3_asr_text_gate_up_swiglu_t1_f32",
        fields=(
            TensorFieldSpec(
                name="x",
                io_kind=IOKind.INPUT,
                role="input",
                contract=TensorContract(dtype="float32", shape=(1, 1, "K")),
            ),
            TensorFieldSpec(
                name="gate_weight",
                io_kind=IOKind.INPUT,
                role="weight",
                contract=TensorContract(dtype="bfloat16", shape=("I", "K")),
            ),
            TensorFieldSpec(
                name="up_weight",
                io_kind=IOKind.INPUT,
                role="weight",
                contract=TensorContract(dtype="bfloat16", shape=("I", "K")),
            ),
            TensorFieldSpec(
                name="output",
                io_kind=IOKind.OUTPUT,
                role="output",
                contract=TensorContract(dtype="float32", shape=(1, 1, "I")),
            ),
        ),
        push_constants=PushConstantSpec(
            size=8,
            fields=(
                PushConstantFieldSpec("K", PushConstantType.UINT32, 0, "K"),
                PushConstantFieldSpec("I", PushConstantType.UINT32, 4, "I"),
            ),
        ),
        dispatch=("I", 1, 1),
    ),
    execution_requirements=ShaderExecutionRequirements(
        subgroup=SubgroupRequirements(required_size=64, require_full_subgroups=True),
        require_storage_buffer_16bit_access=True,
    ),
    source="""
#version 450

#extension GL_EXT_shader_explicit_arithmetic_types_int16 : require
#extension GL_EXT_shader_16bit_storage : require
#extension GL_KHR_shader_subgroup_basic : enable
#extension GL_KHR_shader_subgroup_arithmetic : enable

layout(std430) buffer;

layout(set = 0, binding = 0) buffer restrict readonly XBuffer {
    float x[];
};

layout(set = 0, binding = 1) buffer restrict readonly GateWeightBuffer {
    uint16_t gate_weight[];
};

layout(set = 0, binding = 2) buffer restrict readonly UpWeightBuffer {
    uint16_t up_weight[];
};

layout(set = 0, binding = 3) buffer restrict writeonly OutputBuffer {
    float output_values[];
};

layout(push_constant) uniform PushConstants {
    uint K;
    uint I;
} pc;

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

float bf16_to_f32(uint16_t value) {
    return uintBitsToFloat(uint(value) << 16);
}

void main() {
    const uint k_lane = gl_LocalInvocationID.x;
    const uint local_col = gl_LocalInvocationID.y;
    const uint col = gl_WorkGroupID.x + local_col;

    float gate_acc = 0.0;
    float up_acc = 0.0;
    if (col < pc.I) {
        for (uint k = k_lane; k < pc.K; k += 64u) {
            const float xv = x[k];
            const uint offset = col * pc.K + k;
            gate_acc += xv * bf16_to_f32(gate_weight[offset]);
            up_acc += xv * bf16_to_f32(up_weight[offset]);
        }
    }

    const float gate = subgroupAdd(gate_acc);
    const float up = subgroupAdd(up_acc);
    if (gl_SubgroupInvocationID == 0u && col < pc.I) {
        output_values[col] = (gate / (1.0 + exp(-gate))) * up;
    }
}
""".lstrip(),
)
