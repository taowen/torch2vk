# This file is generated by torch2vk.export. Do not edit by hand.
"""Qwen3-ASR per-head RMS norm for Q/K projections."""

from __future__ import annotations

from torch2vk.runtime.shader import (
    IOKind,
    PushConstantFieldSpec,
    PushConstantSpec,
    PushConstantType,
    ShaderContract,
    ShaderExecutionRequirements,
    ShaderVariant,
    TensorContract,
    TensorFieldSpec,
    ceil_div,
    mul,
)


QWEN3_ASR_TEXT_QK_NORM_F32 = ShaderVariant(
    name="qwen3_asr_text_qk_norm_f32",
    family="qwen3_asr.text",
    contract=ShaderContract(
        class_name="Qwen3AsrTextQkNormF32Program",
        shader_name="qwen3_asr_text_qk_norm_f32",
        fields=(
            TensorFieldSpec(
                name="x",
                io_kind=IOKind.INPUT,
                role="input",
                contract=TensorContract(dtype="float32", shape=(1, "T", "NH")),
            ),
            TensorFieldSpec(
                name="weight",
                io_kind=IOKind.INPUT,
                role="weight",
                contract=TensorContract(dtype="bfloat16", shape=("D",)),
            ),
            TensorFieldSpec(
                name="output",
                io_kind=IOKind.OUTPUT,
                role="output",
                contract=TensorContract(dtype="float32", shape=(1, "T", "NH")),
            ),
        ),
        push_constants=PushConstantSpec(
            size=16,
            fields=(
                PushConstantFieldSpec("T", PushConstantType.UINT32, 0, "T"),
                PushConstantFieldSpec("num_heads", PushConstantType.UINT32, 4, ceil_div("NH", "D")),
                PushConstantFieldSpec("head_dim", PushConstantType.UINT32, 8, "D"),
                PushConstantFieldSpec("eps", PushConstantType.FLOAT32, 12, 1e-6),
            ),
        ),
        dispatch=(mul("T", ceil_div("NH", "D")), 1, 1),
    ),
    execution_requirements=ShaderExecutionRequirements(require_storage_buffer_16bit_access=True),
    source="""
#version 450

#extension GL_EXT_shader_explicit_arithmetic_types_int16 : require
#extension GL_EXT_shader_16bit_storage : require

layout(std430) buffer;

layout(set = 0, binding = 0) buffer restrict readonly XBuffer {
    float x[];
};

layout(set = 0, binding = 1) buffer restrict readonly WeightBuffer {
    uint16_t weight[];
};

layout(set = 0, binding = 2) buffer restrict writeonly OutputBuffer {
    float output_values[];
};

layout(push_constant) uniform PushConstants {
    uint T;
    uint num_heads;
    uint head_dim;
    float eps;
} pc;

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

shared float partial_sumsq[256];

float bf16_to_f32(uint16_t value) {
    return uintBitsToFloat(uint(value) << 16);
}

void main() {
    const uint wg = gl_WorkGroupID.x;
    const uint token = wg / pc.num_heads;
    const uint head = wg - token * pc.num_heads;
    const uint tid = gl_LocalInvocationID.x;
    if (token >= pc.T) {
        return;
    }

    const uint NH = pc.num_heads * pc.head_dim;
    const uint base = token * NH + head * pc.head_dim;

    float sumsq = 0.0;
    for (uint d = tid; d < pc.head_dim; d += 256u) {
        const float v = x[base + d];
        sumsq += v * v;
    }
    partial_sumsq[tid] = sumsq;
    barrier();

    for (uint stride = 128u; stride > 0u; stride >>= 1u) {
        if (tid < stride) {
            partial_sumsq[tid] += partial_sumsq[tid + stride];
        }
        barrier();
    }

    const float inv_rms = inversesqrt(partial_sumsq[0] / float(pc.head_dim) + pc.eps);
    for (uint d = tid; d < pc.head_dim; d += 256u) {
        output_values[base + d] = x[base + d] * inv_rms * bf16_to_f32(weight[d]);
    }
}
""".lstrip(),
)
