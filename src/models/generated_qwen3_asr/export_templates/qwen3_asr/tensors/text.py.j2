"""Generated Qwen3-ASR text frame tensor scaffold."""

from __future__ import annotations

from dataclasses import dataclass

from torch2vk.runtime.logical import LogicalTensor

from models.generated_qwen3_asr.tensors.text_layer import GeneratedQwen3AsrTextLayerTensors


@dataclass(frozen=True, slots=True)
class GeneratedQwen3AsrTextPrefillTensors:
    input_ids: LogicalTensor
    attention_mask: LogicalTensor
    input_features: LogicalTensor | None
    feature_attention_mask: LogicalTensor | None
    position_ids: LogicalTensor
    rope_cos: LogicalTensor
    rope_sin: LogicalTensor
    audio_features: LogicalTensor
    audio_scatter_mask: LogicalTensor
    embed_tokens_weight: LogicalTensor
    inputs_embeds: LogicalTensor
    layers: tuple[GeneratedQwen3AsrTextLayerTensors, ...]
    norm_weight: LogicalTensor
    final_norm: LogicalTensor
    lm_head_weight: LogicalTensor
    logits: LogicalTensor


@dataclass(frozen=True, slots=True)
class GeneratedQwen3AsrTextDecodeTensors:
    input_ids: LogicalTensor
    attention_mask: LogicalTensor
    position_ids: LogicalTensor
    rope_cos: LogicalTensor
    rope_sin: LogicalTensor
    cache_position: LogicalTensor
    embed_tokens_weight: LogicalTensor
    inputs_embeds: LogicalTensor
    layers: tuple[GeneratedQwen3AsrTextLayerTensors, ...]
    norm_weight: LogicalTensor
    final_norm: LogicalTensor
    lm_head_weight: LogicalTensor
    lm_head_select_scratch: LogicalTensor
    logits: LogicalTensor


@dataclass(frozen=True, slots=True)
class GeneratedQwen3AsrTokenSelectTensors:
    eos_token_ids: LogicalTensor
    next_token: LogicalTensor
    done: LogicalTensor
    generated_tokens: LogicalTensor


@dataclass(frozen=True, slots=True)
class GeneratedQwen3AsrTokenStoreTensors:
    next_token: LogicalTensor
    token_index: LogicalTensor
    done: LogicalTensor
    generated_tokens: LogicalTensor
    generated_length: LogicalTensor
    stopped: LogicalTensor


@dataclass(frozen=True, slots=True)
class GeneratedQwen3AsrTextTensors:
    prefill: GeneratedQwen3AsrTextPrefillTensors
    decode: GeneratedQwen3AsrTextDecodeTensors
    token_select: GeneratedQwen3AsrTokenSelectTensors
    token_store: GeneratedQwen3AsrTokenStoreTensors


def declare_generated_qwen3_asr_text_tensors(
    *,
    prompt_length: int,
    audio_tokens: int,
    max_sequence_length: int,
    hidden_size: int = {{ defaults.text_hidden_size }},
    intermediate_size: int = {{ defaults.text_intermediate_size }},
    vocab_size: int = {{ defaults.text_vocab_size }},
    decoder_layers: int = {{ defaults.text_decoder_layers }},
    num_attention_heads: int = {{ defaults.text_num_attention_heads }},
    num_key_value_heads: int = {{ defaults.text_num_key_value_heads }},
    head_dim: int = {{ defaults.text_head_dim }},
    eos_token_ids: tuple[int, ...] = {{ defaults.eos_token_ids|pyrepr }},
    audio_features: LogicalTensor | None = None,
    pytorch_input_features_shape: tuple[int, ...] | None = None,
    pytorch_feature_attention_mask_shape: tuple[int, ...] | None = None,
) -> GeneratedQwen3AsrTextTensors:
    raise NotImplementedError(
        "Generated scaffold only: fill prefill/decode/token-select/token-store tensor declarations."
    )
